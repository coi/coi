// Coi Built-in Boolean Type
// Boolean (true/false)

@builtin
type bool {
    // Casts inline to coi:: overloads defined in the codegen prelude
    @inline("coi::to_int(${this})")
    def toInt(): int

    @inline("coi::to_string(${this})")
    def toString(): string
}
